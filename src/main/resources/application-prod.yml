spring:

  datasource:
    hikari:
      maximumPoolSize: 5

  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect


hnnotify:
  initialDelay: 30000
  maxStoriesToFetch: 100
  timeBetweenFetches: 30000
  cleanup:
    targetNumberOfRows: 9000
    timeBetween: 300000
    initialDelay: 5000
